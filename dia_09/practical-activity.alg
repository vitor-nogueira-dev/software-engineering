algoritmo "Procedimento multiplicar"
procedimento multiplicar

var 
  resultado: inteiro
inicio
  resultado <- a * b
  res <- resultado
fimprocedimento

var 
  res, a, b: inteiro
inicio
  a <- 5
  b <- 3
  multiplicar
  escreva(res)
fimalgoritmo

